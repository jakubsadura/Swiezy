/*
 * here is where system computed values get stored these values should only
 * change when the target compile platform changes
 */


#cmakedefine CILABMACROSAPI_BUILD_SHARED_LIBS
#ifdef CILABMACROS_BUILD_SHARED_LIBS
#define CILABMACROSDLL
#else
#define CILABMACROSSTATIC
#endif
